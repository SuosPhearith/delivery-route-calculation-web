import ZoneComponent from "@/components/resources/admin/zone/ZoneComponent";
import React from "react";

const page = () => {
  return (
    <main>
      <ZoneComponent />
    </main>
  );
};

export default page;
