import SizeComponent from "@/components/resources/admin/SizeComponent";
import React from "react";

const page = () => {
  return (
    <main>
      <SizeComponent />
    </main>
  );
};

export default page;
